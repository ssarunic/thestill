# Thestill Specs

Technical specifications for thestill. Plans are gated and versioned inline;
references are evergreen.

This index is the canonical entry point. When adding, completing, or deprecating
a spec, update the relevant table below.

Each spec carries a stable numeric ID, which is also the filename prefix. IDs
are assigned on creation and never reused — moving a spec between tables
(e.g., Active → Completed) does **not** renumber it and does **not** rename
the file. When adding a new spec, assign the next available ID
(`max(current IDs) + 1`) and use it as the filename prefix.

## Reference

Evergreen documents describing how the system works today. Read these first
when getting oriented.

| ID | Spec | Summary |
|---:|---|---|
| 01 | [architecture](01-architecture.md) | Layered architecture, dependency flow, design patterns |
| 02 | [api-reference](02-api-reference.md) | REST API endpoints, response envelope, pagination format |
| 03 | [error-handling](03-error-handling.md) | Exception hierarchy, fail-fast patterns, error classification |
| 04 | [testing](04-testing.md) | Coverage targets, test types, type-checking standards |

## Active plans

Planned or in-progress work. Each spec carries its own `Status` header
describing current phase and gates.

| ID | Spec | Status | Summary |
|---:|---|---|---|
| 05 | [docker-deployment](05-docker-deployment.md) | 🚧 Active development | Slim cloud-only Docker image for RPi5, multi-stage `:slim`/`:full` targets, Dalston-only default |
| 06 | [authentication](06-authentication.md) | 🚧 Active development | Opt-in auth system: zero-friction self-hosted, multi-tenant-ready for hosted |
| 07 | [multi-user-web-app](07-multi-user-web-app.md) | 📝 Draft | Multi-user podcast tracking web app, "process once, deliver to many" |
| 08 | [multi-phase-transcript-cleaning](08-multi-phase-transcript-cleaning.md) | 🗄 Archived | Superseded by #18 |
| 09 | [single-user-web-ui](09-single-user-web-ui.md) | 🚧 Active development | Single-user web UI: dashboard, podcast management, pipeline visibility |
| 10 | [queue-viewer](10-queue-viewer.md) | 🚧 Active development | Task queue viewer page: pending/processing/retry tasks with bump-to-front |
| 11 | [task-queue-monitor](11-task-queue-monitor.md) | 🚧 Active development | Unified task queue monitor replacing the Failed Tasks page |
| 12 | [whisperx-chunk-progress-tracking](12-whisperx-chunk-progress-tracking.md) | 🚧 Active development | Capture WhisperX stdout progress output and convert to real-time callbacks |
| 18 | [segment-preserving-transcript-cleaning](18-segment-preserving-transcript-cleaning.md) | 🚧 Active development | Per-segment cleanup with word timestamps preserved; prerequisite for richer media player |
| 19 | [refresh-performance](19-refresh-performance.md) | 🚧 Active development | Profile `thestill refresh`, parallelize feed fetching, add conditional GET, design automated scheduler |
| 20 | [parallel-task-queues](20-parallel-task-queues.md) | 🚧 Active development | Per-stage worker pools so a slow transcribe task no longer blocks a fast clean task; stage-swimlane UI |
| 21 | [episode-processing-indicator](21-episode-processing-indicator.md) | 🚧 Active development | Show "currently processing" badge on episode cards in lists; frontend-only via queue-tasks hook |
| 22 | [floating-media-player](22-floating-media-player.md) | 🚧 Active development | Persistent audio playback across route changes via `PlayerContext` + mini player in `Layout`; seam for future custom player |
| 24 | [word-level-transcript-highlighting](24-word-level-transcript-highlighting.md) | 🗄 Archived | Superseded by #38 |
| 26 | [pre-deploy-security-checklist](26-pre-deploy-security-checklist.md) | 📋 Planned (2026-04-23) | LLM-runnable + runtime smoke-test checklist that must return GO before any deploy; regression harness for spec #25 |
| 28 | [corpus-search-and-entities](28-corpus-search-and-entities.md) | 🚧 Phases 0–4 complete; Phase 5 (entity pages + augmented reader) in progress | Native person/company/topic entity index over real podcast corpus; sqlite-vec hybrid search (Phase 2.10), DLQ-separated entity branch with CI latency budgets (Phase 3), and `⌘K` command bar + `/search` slug routes (Phase 4) all shipped. Phase 5: 7 of 20 reader affordances landed, entity-page polish + remaining affordances + Playwright suite still owed |
| 29 | [per-user-inbox-fanout](29-per-user-inbox-fanout.md) | 🚧 Active development | Per-user Inbox replacing shared "recent episodes" list: write-fan-out on publish, seed-on-follow, per-user read/save/dismiss state; sets the steady-state delivery model for full automation |
| 30 | [mcp-anchors-and-entity-discovery](30-mcp-anchors-and-entity-discovery.md) | 🚧 Active development | Seven thin MCP tools (anchor queries + entity discovery) so the LLM-harness surface catches up to the web entity surface introduced by spec #28 PRs #60/#61 |
| 31 | [import-arbitrary-episodes](31-import-arbitrary-episodes.md) | 🚧 Active development | Paste any URL (YouTube / RSS episode / audio file) → episode lands in user's inbox immediately and runs through the existing pipeline; synthetic-podcast parents in v1, no follow side-effect |
| 32 | [episodes-as-first-class](32-episodes-as-first-class.md) | 📝 Draft | Lift `episodes.podcast_id NOT NULL` and add a many-to-many `collection_memberships` table; same content shared across cross-posts, user playlists, entity-pinned feeds; additive migration with a dual-write window |
| 33 | [narrated-digest](33-narrated-digest.md) | ✅ Complete | Single-anchor news-style readout replacing the concatenated digest; theme-grouped segments with verbatim quote clips, capped by user-chosen spoken duration; markdown + TTS-ready JSON script |
| 34 | [briefing-audio-and-feeds](34-briefing-audio-and-feeds.md) | 📝 Draft | Render #33's script to MP3 with TTS anchor + spliced original-audio quote clips; deliver via private token-protected personal podcast RSS feed (Apple / Overcast / Pocket Casts), in-app player, and direct download |
| 36 | [per-user-digest-from-inbox](36-per-user-digest-from-inbox.md) | 🚧 Active development (digest retirement done 2026-07-07) | Wire the morning briefing to select from each user's inbox since their last briefing; replaces the global recent-episode window post-#29; unblocks per-user audio in #34. Retirement executed: digest CLI/API/UI/MCP/repos/tables removed (alembic 0003), `DigestGenerator`→`BriefingScriptGenerator`, narration rekeyed to `briefing_id` (`POST /api/briefings/{id}/narrate`) |
| 37 | [substack-import-resolver](37-substack-import-resolver.md) | 📝 Draft | Add `SubstackResolver` so pasted Substack post URLs (open.substack.com, `*.substack.com`, custom domains) resolve to embedded podcast audio; reuses #31's canonical-id + auto-add-parent path |
| 38 | [karaoke-word-highlighting](38-karaoke-word-highlighting.md) | 📝 Draft | Karaoke-style smooth-wipe word highlighting during playback; CSS gradient + rAF driver, opt-in toggle, graceful fallback when words missing; supersedes #24 |
| 39 | [video-alternate-enclosure-player](39-video-alternate-enclosure-player.md) | 📝 Draft | Render `<podcast:alternateEnclosure>` video variants (YouTube embeds first, room for mp4/HLS later) on the Episode Detail page; audio pipeline unchanged |
| 41 | [llm-prohibited-content-fallback](41-llm-prohibited-content-fallback.md) | 🚧 Active development | Per-batch pass-through when Gemini returns `PROHIBITED_CONTENT`; structured-output path now surfaces real `finish_reason`. Option A (per-batch model fallback to Claude / Mistral) still owed |
| 42 | [robustness-and-failure-mode-hardening](42-robustness-and-failure-mode-hardening.md) | 📝 Draft (2026-05-21) | Post-mortem of the silent tz-naive/aware refresh outage → seven named failure modes (errors-as-empty-results, checkpoint-before-durability, mixed-tz, silent fleet degradation, consistent-mock tests, parallel-path drift, unsanitized-LLM-output), enforcement plan, and PR review checklist. Phase 1 = gate ETag on success + narrow excepts + tz-aware Pydantic boundary |
| 43 | [aws-hosting](43-aws-hosting.md) | 📝 Draft (2026-05-22) | Phase-1 hosted beta on AWS (~1000 podcasts / ~4000 episodes/mo): app on a single EC2 `t4g.xlarge` + in-process worker, Dalston co-located on a `g4dn.xlarge` GPU (Spot-friendly), RDS Postgres + pgvector, S3 artifacts with a 14-day audio-cache lifecycle, S3 Gateway endpoint (no NAT). Gated on #44; HA later = Multi-AZ flip. ~$265–540/mo |
| 45 | [entity-page-enrichment](45-entity-page-enrichment.md) | 🚧 Active development | Make person/company entity pages informative & fun. Three tiers: T0 = free structured facts/visuals from the Wikidata QID we already hold (photo/logo, vital stats, founder↔company cross-links) + Wikipedia lead + internal relationship graph; T1 = cached LLM narrative (why-they-matter, fun facts, founding story, quote synthesis); T2 = external/paid (financials, news↔spike correlation, predictions, audio supercut). Additive `entity_enrichment` table + one nullable API field; gated on QID (= notability); per-#42 failure model (transient ≠ "no data"). Sentiment explicitly cut |
| 46 | [related-episodes-scaling](46-related-episodes-scaling.md) | 🚧 In progress (2026-05-30) | Scale the #28 §5.2 "Related episodes" rail from O(N²) all-pairs + O(M)-chunk load to sub-quadratic, without changing relevance. T0 = materialise episode centroids in `ChunkWriter` (+`episode_vec` ANN), kills the chunk-load wall; T2 = candidate-gen (vector-ANN ∪ BM25-on-top-IDF-terms) → blend rerank, retires the N×N matrix for full *and* incremental; T3 = `COMPUTE_RELATED` pipeline stage (mirrors `REBUILD_COOCCURRENCES`) with persisted IDF, forward + bounded reverse update. Tier 1 (block the dense matrix) deliberately skipped; bge model swap rejected (#28) |
| 47 | [auto-entity-enrichment-stage](47-auto-entity-enrichment-stage.md) | 🚧 In progress (2026-05-30) | Wire #45 Tier-0 enrichment into the pipeline so newly-resolved entities get their photo/bio/vital-stats without a manual `enrich-entities` run. Adds `ENRICH_ENTITIES` as the terminal entity-branch stage (mirrors #46 `COMPUTE_RELATED`): coalesced under one lock, scoped via existing `entity_ids_needing_enrichment(episode_id=…)`, network outside any txn, `enrichment_max_per_task` cap, FM-1 per-entity isolation. Runs LAST so its latency never delays search/related. Rejected: inline-into-resolve (blocks the single SQLite writer, couples throughput to Wikipedia uptime). Scheduled batch stays for retries/staleness |
| 48 | [refresh-feed-stage](48-refresh-feed-stage.md) | 🚧 Active development | Promote per-feed refresh from the monolithic in-process `refresh_feeds` batch (#19) to a queued `REFRESH_FEED` pipeline stage (#20). One task = one feed; reuses `_refresh_single_podcast`, persists per-feed (incremental visibility), enqueues `DOWNLOAD` per new episode. Wins: horizontal scale, independent retry/DLQ, per-feed cadence — not raw speed. Central tension: episode-centric queue → nullable `episode_id` + new `podcast_id` column (rejected: overloading `episode_id`). New feed-scoped failure domain mirrors #28 §6, preserves FM-2 (no checkpoint-on-failure) + FM-4 (silent-fleet via DLQ depth). Per-host politeness moves to a shared limiter. Ships dark behind `REFRESH_VIA_QUEUE`; inline path stays single-user default |
| 49 | [queue-auto-healing-and-recovery](49-queue-auto-healing-and-recovery.md) | 📝 Draft (2026-06-23) | Make the task queue dig itself out of correlated infra outages instead of leaving terminal `failed`/`dead` rows for manual retry. Traced a 101-task pile-up to one defect: the queue treats "shared dependency down" (DNS `[Errno 8]`, `runtime_unavailable`) identically to "this work is bad", so every in-flight task burns its 3-strike budget inside the ~3.5-min backoff window (#16) and dies together; nothing ever re-examines a terminal row. Four layers: (1) per-stage circuit breaker pauses dequeue during an outage so budgets aren't spent; (2) `INFRA_PATTERNS` attribution so infra failures don't count against `max_retries`; (3) a healer loop over `failed`-only (never `dead`), bounded by a `heal_attempts` cap; (4) generalize #48's idempotent-restart carve-out to all re-runnable stages. Layers 1+3 alone self-resolve the incident. Ships behind `QUEUE_AUTO_HEAL` |
| 50 | [scheduled-briefings](50-scheduled-briefings.md) | 🚧 Implemented Phases 1–3 (2026-07-07); Phase 4 pending #34 | Per-user scheduled briefing generation — the auto-cadence follow-up #36 deferred. Each user sets `hour_local`, `daily`/`weekly` (+weekday), and IANA timezone on a new `user_briefing_schedules` row; a `BriefingScheduler` daemon thread (mirrors #48: materialized `next_run_at`, indexed due-scan, advance-before-generate conditional claim) calls `BriefingService.generate_for_user` at each user's slot. Cursor/throttle/empty-window semantics from #36 unchanged; catch-up after downtime fires once (widened cursor), never replays missed slots. DST via `zoneinfo` (FM-3), per-user failure isolation (FM-1). Opt-in; lazy on-open generation stays the default. `GET`/`PUT /api/briefings/schedule` |
| 51 | [briefing-email-delivery](51-briefing-email-delivery.md) | 🚧 Implemented Phases 1–3 (2026-07-08); Phase 4 (SES bounce hardening) pending #43 | Email the #50 scheduled briefing so the morning ritual starts in the user's inbox. Core move: decouple "generated" from "delivered" — a `briefing_deliveries` table (`UNIQUE(briefing_id, channel)` = send-once anchor) with its own pending→sent/failed state machine and bounded backoff, so the lazy-7:30 + scheduled-8:00 interaction emails exactly once and SMTP outages never lose a briefing or block generation (FM-1/FM-4). `email_enabled` opt-in on the schedule row; HTML+text rendering with `PUBLIC_BASE_URL` absolute links; signed one-click unsubscribe (RFC 8058, legal req); `EmailSender` ABC — SMTP default for self-host, SES for #43 hosted, `EMAIL_PROVIDER=none` global off. Empty window sends nothing (weekly exception an open question) |
| 52 | [inbox-reader-overlay](52-inbox-reader-overlay.md) | ✅ Phase 1 implemented (2026-07-08); Phases 2–3 pending | Open inbox episodes in a reader overlay above the still-mounted inbox list (Gmail-thread model) instead of a full route change into the Podcasts hierarchy. React Router background-location pattern keeps the canonical episode URL (refresh/share degrade to the standalone page, no duplicate URLs); Esc/scrim/`← Inbox` = `history.back()` restoring scroll + poll + query state. Phase 1 shipped the shared `EpisodeReader` extraction, overlay chrome, background-location-aware nav highlight, and scroll-container-aware `useReadingPosition`. Frontend-only; Phase 2 adds read/save/dismiss buttons (completing #29 Phase 3) and j/k triage |
| 53 | [eval-runs-and-summary-rubric](53-eval-runs-and-summary-rubric.md) | 🚧 Implemented Phases 1–3 (2026-07-10); Phase 4 future | Make LLM-judge eval runs first-class and append-only: `data/evaluations/runs/<run_id>/` with a manifest recording judge (provider/model/temp), rubric (name/version/`prompt_sha256`), artifact content hashes, per-item ok/failed status + denormalized scores. Judge pinned independently of the pipeline LLM (`EVAL_JUDGE_*`, dated snapshot ids, cross-family vs the producing model to dodge self-preference bias). New `thestill eval run/list/show/compare` group; compare classifies what differs (judge vs pipeline vs confounded) before showing per-dimension deltas; `--samples N` for judge-variance visibility. Existing raw/clean rubric prompts move to a versioned registry (hash-guarded by test); legacy `evaluate-*` commands deprecated to wrappers. Phase 3 adds the missing **summary rubric** (coverage/faithfulness/attribution + deterministic checks: section presence, timestamp bounds — computed in Python, not judged) and a committed golden episode set. FM-1/3/4/5/7 addressed; DB storage, CI gate, briefing rubric explicitly deferred |
| 54 | [summary-segment-citations](54-summary-segment-citations.md) | ✅ Implemented (2026-07-09) | Turn inert `[49:30]` summary citations into clickable pointers into the transcript. Invert the model: a deterministic resolver (Option B — LLM keeps emitting timestamps, no prompt change) maps each `[MM:SS]` to the `AnnotatedSegment` containing it via `playback_s − playback_time_offset_seconds` (the LLM sees playback-time markdown from `to_blended_markdown`), and stores the **durable `source_segment_ids` + `cited_playback_s`** anchor — with positional `segment_id` kept only as a re-resolvable hint — in a `_summary.citations.json` sidecar (mirrors #18's clean-transcript sidecar). Markdown body stays readable; citations become `[49:30](?t=2970&cite=cN)` app deep links (reuses #23's `?t=`, dodges ReactMarkdown's `urlTransform` protocol filter); the **displayed timestamp is preserved verbatim, never snapped**. Rewrite is a conservative Markdown-aware pass, not whole-doc regex: fenced code, inline code, existing links/images/reference links, and ranges are skipped. Frontend target is `EpisodeReader` (tab+seek moved there in #52) with a new explicit `scrollToSegmentId` contract on `SegmentedTranscriptViewer`. Staleness guard: sidecar carries `summary_sha256` + transcript metadata; API drops citations on mismatch and re-resolves compatible algorithm-version drift server-side. One shared resolve-and-persist helper across both summarize paths (FM-6); per-citation degrade-to-text (FM-1); range-validate untrusted LLM output (FM-7). Includes `thestill resolve-summary-citations` for no-LLM backfill. |
| 55 | [briefing-readiness-gate](55-briefing-readiness-gate.md) | 📝 Draft (2026-07-10) | Stop briefings racing the pipeline (the 2026-07-10 one-episode briefing: cut at 10:16:36, nine episodes landed 10:19–10:29). Before cutting, compute a **wait-set** — episodes from podcasts the user follows, `pub_date` before the cutoff (scheduled slot hour or lazy `now()`), discovered but not yet in the user's inbox, with a still-progressing task chain (permanently failed chains never block) — and defer generation while it's non-empty, re-checking on the #50 tick, up to `BRIEFING_READINESS_GRACE_MINUTES` (default 60) anchored at **actual fire time** (sleep catch-up keeps its full grace). At deadline, cut unconditionally; stragglers roll into the next briefing via #36 cursor semantics (gate decides *when*, never *what* — no-loss invariant untouched; cursor never moves on deferral). Lazy path returns `202 briefing_pending` + pending count; inbox shows "catching up — N processing" with a `?force=true` Generate-now escape hatch. Fail-open on readiness-query errors (FM-1); `briefing_grace_expired` logs abandoned count (FM-4). No schema changes, no new threads |
| 56 | [related-episodes-constant-time](56-related-episodes-constant-time.md) | 📝 Draft (2026-07-10) | Tier 4 of #46, from the production finding that "incremental" `compute-related` recomputes rails for 92% of the corpus per summarize batch (`seed=1, affected=1344`, avg 19 min — the pipeline's slowest stage). Invariant: per-episode work is O(k), k fixed (150), never O(corpus). Four changes: (1) pool-independent score calibration — replace per-pool min-max with fixed squash constants so stored scores are globally comparable (the prerequisite); (2) fixed-k recall pools decoupled from the full-rebuild cap; (3) reverse rails become O(1) score-vs-stored-worst merges, never pool recomputes; (4) `episode_features` store (top-50 sparse TF-IDF, entity-id arrays) so the rail path never touches the 309k-row chunks table, with the lexical leg moving to GIN term-overlap. Sampled+versioned IDF refits and a budgeted staleness sweep replace routine full rebuilds. Phased: bleeding-stop (pool k only) → calibrated merge → feature store → maintenance loop; rank-parity harness gates each cutover; holds at 1000× corpus |
| 57 | [eea-top-podcast-regions](57-eea-top-podcast-regions.md) | 📝 Draft (2026-07-13) | Expand top-podcast charts from `us`/`gb` to the whole EEA (EU 27 + Iceland, Liechtenstein, Norway) so single-market users see their own country's chart instead of falling back to US/UK. Feature is already region-generic — supported regions are glob-discovered from `data/top_podcasts_<code>.json` and seeded into `top_podcasts_meta`, no hardcoded allowlist — so the work is (1) generate 29 new data files via `scripts/build_top_podcasts.py --region <code>` (full enrichment, run sequentially), (2) add the EEA labels/flags to the two decoupled frontend lists (`Settings.tsx` `REGIONS`, `regions.ts` `FLAG`), kept in the same order to avoid FM-6 drift. `li` excluded — Apple has no Liechtenstein storefront chart (RSS returns 0). Implementation added the missing Postgres glob-seeder (SQLite had it; Postgres didn't — FM-6) so "drop a JSON file" works on the hosted backend; needs the DB at alembic head (`image_url`, `0005`). GB retained as a non-EEA first-class region |
| 58 | [original-language-summaries](58-original-language-summaries.md) | 📝 Draft (2026-07-13) | Summaries follow the podcast's `language` instead of always being English, with a reader toggle between original language and the user's default. Everything upstream already runs in-language (transcription/cleaning pass `podcast.language`); only the summarizer prompt hardcodes British English ([post_processor.py:69](../thestill/core/post_processor.py#L69)) — so original-language is the *natural* output and English is the current extra step. Model: original-language summary is the base artifact (keeps `episodes.summary_path`), the default-language version is a **lazy, cached** translation stored as a sibling `…_summary.<lang>.md` file — zero extra cost on the common English path, pay-on-first-toggle for foreign feeds. Adds `?lang=` to the summary endpoint, a conditional toggle in `EpisodeReader` shown only when podcast≠default language, and browser-locale-derived default (v1; `User.language` setting deferred to v2). Citations (#54) must survive translation |

## Completed

Shipped work. Kept for historical context and rollback reference.

| ID | Spec | Completed | Summary |
|---:|---|---|---|
| 44 | [postgres-migration](44-postgres-migration.md) | 2026-07-03 | Full SQLite→Postgres support behind `DATABASE_URL` (native uuid/timestamptz/jsonb/pgvector schema, all 8 repos + queue `SKIP LOCKED` + pgvector search, factory-wired entry points, alembic, CI Postgres service, mirror+promotion migration tooling); production cut over 2026-07-03 |
| 35 | [pluggable-file-storage](35-pluggable-file-storage.md) | 2026-05-13 | `FileStorage` abstraction + `S3FileStorage` backend (PR #93); per-artifact migrations: digests/corpus/external transcripts (#93), `podcast_service` reads (#95), transcribers + audio pipeline (#96). Phase 4 (presigned URLs) deferred to spec #34; Phase 5 (Terraform/CDK) lives outside the code path |
| 40 | [storage-routing-ephemeral-vs-persistent](40-storage-routing-ephemeral-vs-persistent.md) | 2026-05-13 | Settles #35's per-artifact-routing question with two carve-outs: pending transcription ops move from JSON files to SQLite (PR #94); debug feeds keep direct `Path` I/O. Downsampled WAV stays in main backend |
| 13 | [multi-user-shared-podcasts](13-multi-user-shared-podcasts.md) | 2026-01-21 | Phase 1 (follow/unfollow) shipped: shared processing across users |
| 25 | [security-audit-and-hardening](25-security-audit-and-hardening.md) | 2026-04-27 | All 27 findings closed across phases 1–5 (XXE, SSRF, webhook auth, JWT, CORS, supply chain, race conditions, etc.) |
| 27 | [add-podcast-search-discoverability](27-add-podcast-search-discoverability.md) | 2026-04-27 | Search-or-paste Add Podcast modal filtering regional top-500 live; data path pre-shapes a future free-tier gate |
| 14 | [dry-refactoring-plan](14-dry-refactoring-plan.md) | 2026-01-13 | Eliminated ~210 lines of duplication across 6 refactoring phases |
| 15 | [mistral-llm-provider](15-mistral-llm-provider.md) | 2026-01-12 | Added Mistral AI as fifth LLM provider with full feature parity |
| 16 | [full-pipeline-and-failure-handling](16-full-pipeline-and-failure-handling.md) | 2026-01-07 | Full-pipeline execution + transient/fatal error split + DLQ |
| 17 | [pylint-fixes](17-pylint-fixes.md) | 2026-01-15 | Pylint score lifted to 9.19/10, zero E-level errors remaining |
| 23 | [transcript-playback-sync](23-transcript-playback-sync.md) | 2026-04-20 | Active-segment highlight, follow-playback toggle, click-to-seek, deep-linked timestamps, filler reveal, in-transcript search with collapse/next-prev |

## Status legend

| Marker | Meaning |
|---|---|
| 📖 Reference | Evergreen doc describing current system |
| 💡 Proposal | Problem framed, solution space being explored |
| 📝 Draft | Solution drafted, not yet scheduled |
| 📋 Planned | Scheduled, ready to execute, not started |
| 🚧 Active development | In progress, phases partially complete |
| ✅ Complete | Shipped and verified |
| 🗄 Archived | Superseded or abandoned (kept for history) |

## Conventions for new specs

- **ID**: assign the next available ID (highest current ID + 1). IDs are
  zero-padded to two digits (`01`, `02`, …) and remain stable for the life of
  the spec. Never reuse an ID, even for an archived/abandoned spec. Switch to
  three digits when crossing 99.
- **Filename**: `NN-kebab-case-descriptive.md`, where `NN` is the spec's ID.
  Example: `05-docker-deployment.md`. No date prefix. The ID prefix is part of
  the filename and is never changed once assigned — not even when status
  changes or the spec is archived.
- **Status header**: every spec starts with a table or bullet list containing
  `Status`, `Created`, `Updated`, and (for plans) `Priority`. See
  [05-docker-deployment.md](05-docker-deployment.md) for the reference format.
- **Linking**: use relative paths. File references inside a spec should use
  `../` to reach repo-root files — e.g., `[cli.py](../thestill/cli.py)`.
  Spec-to-spec references use the full prefixed filename — e.g.,
  `[06-authentication.md](06-authentication.md)`.
- **Index entry**: add a row to this file in the appropriate table as part of
  the same PR that creates the spec. Move the row between tables as status
  changes; the ID and filename both travel with the row unchanged.
- **Code citations**: reference file:line when pointing at specific code, for
  example `[cli.py:1458](../thestill/cli.py#L1458)`.
- **Referring to a spec**: use `spec #NN` in PRs, commits, and conversation —
  short and unambiguous, and easy to search for. The full filename is fine
  too, but the ID is the canonical short handle.
